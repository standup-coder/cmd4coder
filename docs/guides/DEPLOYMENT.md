# GitHub Pages 部署指南

本文档将指导你如何为 cmd4coder 项目启用 GitHub Pages。

## 📋 前提条件

- 拥有 GitHub 账户
- 对仓库有管理员权限
- 已将 `docs/` 目录推送到 GitHub 仓库的 main 分支

## 🚀 部署步骤

### 第一步：确认文件已推送

确保 `docs/` 目录下的所有文件已提交并推送到 GitHub：

```bash
# 查看状态
git status

# 添加所有文件
git add docs/

# 提交
git commit -m "Add GitHub Pages for cmd4coder"

# 推送到远程仓库
git push origin main
```

### 第二步：启用 GitHub Pages

1. **打开仓库设置**
   - 访问你的 GitHub 仓库页面
   - 点击页面顶部的 **Settings** (设置) 选项卡

2. **找到 Pages 设置**
   - 在左侧边栏中，向下滚动找到 **Pages** 选项
   - 点击进入 GitHub Pages 设置页面

3. **配置部署源**
   - 在 **Source** (源) 部分：
     - **Branch**: 从下拉菜单选择 `main`
     - **Folder**: 从下拉菜单选择 `/docs`
   - 点击 **Save** (保存) 按钮

4. **等待部署完成**
   - GitHub 会自动开始构建和部署网站
   - 通常需要 1-2 分钟
   - 页面会显示 "Your site is ready to be published at..." 或 "Your site is live at..."

### 第三步：访问网站

部署成功后，你的网站将可以通过以下地址访问：

```
https://[你的GitHub用户名].github.io/cmd4coder/
```

例如，如果你的 GitHub 用户名是 `username`，那么访问地址是：
```
https://username.github.io/cmd4coder/
```

## ✅ 验证部署

### 检查部署状态

1. 在 GitHub Pages 设置页面，查看部署状态
2. 如果显示绿色的 "Your site is live"，说明部署成功
3. 点击提供的链接访问你的网站

### 查看部署历史

1. 回到仓库主页
2. 点击 **Actions** 选项卡
3. 查看 `pages build and deployment` 工作流
4. 绿色勾号表示部署成功，红色叉号表示部署失败

### 测试网站功能

访问网站后，测试以下功能：

- [ ] 页面正常加载，样式显示正确
- [ ] 导航链接能够平滑滚动到对应章节
- [ ] 代码复制按钮正常工作
- [ ] 所有外部链接（GitHub、文档等）正确跳转
- [ ] 移动端显示正常（可以使用浏览器开发者工具测试）
- [ ] 返回顶部按钮在滚动时显示并正常工作

## 🔧 常见问题

### Q: 页面显示 404 错误

**可能原因**：
- `docs/` 目录未正确推送到 main 分支
- GitHub Pages 配置的路径不正确
- 缓存问题

**解决方法**：
1. 检查 `docs/` 目录是否存在于 main 分支
2. 确认 Pages 设置中选择的是 `/docs` 而不是 `/root`
3. 等待几分钟后重试，或清除浏览器缓存

### Q: 样式未加载，页面显示不正常

**可能原因**：
- CSS 文件路径不正确
- 文件未正确推送

**解决方法**：
1. 检查 `docs/css/` 目录下的所有 CSS 文件是否存在
2. 查看浏览器控制台是否有 404 错误
3. 确认 `index.html` 中的 CSS 引用路径是否正确（相对路径）

### Q: JavaScript 功能不工作

**可能原因**：
- JS 文件未加载
- 浏览器兼容性问题

**解决方法**：
1. 检查 `docs/js/main.js` 文件是否存在
2. 打开浏览器控制台查看是否有 JavaScript 错误
3. 尝试在不同浏览器中测试

### Q: 部署后修改未生效

**可能原因**：
- 修改未推送到 GitHub
- 浏览器缓存
- GitHub Pages 更新延迟

**解决方法**：
1. 确认修改已提交并推送：`git push origin main`
2. 清除浏览器缓存或使用无痕模式访问
3. 等待 1-2 分钟让 GitHub Pages 重新部署
4. 在 Actions 选项卡中查看最新的部署状态

## 🎨 自定义域名（可选）

如果你想使用自定义域名（如 `cmd4coder.dev`）：

### 步骤 1：配置 DNS

在你的域名服务商处配置 CNAME 记录：

```
类型: CNAME
名称: www (或你想要的子域名)
值: [你的GitHub用户名].github.io
```

### 步骤 2：添加 CNAME 文件

在 `docs/` 目录创建 `CNAME` 文件：

```bash
echo "www.cmd4coder.dev" > docs/CNAME
git add docs/CNAME
git commit -m "Add custom domain"
git push origin main
```

### 步骤 3：在 GitHub Pages 设置中配置

1. 在 GitHub Pages 设置页面
2. 在 **Custom domain** 字段输入你的域名
3. 点击 **Save**
4. 等待 DNS 生效和 SSL 证书颁发（可能需要 24 小时）

## 📊 性能优化建议

### 1. 启用 HTTPS

GitHub Pages 默认支持 HTTPS，建议勾选 **Enforce HTTPS** 选项。

### 2. 压缩资源（已优化）

当前实现已经采用了最佳实践：
- 使用系统字体，避免外部字体加载
- SVG 图标内联，减少 HTTP 请求
- CSS 和 JS 文件已分离但未压缩（可根据需要压缩）

### 3. 监控性能

使用 Lighthouse 或 PageSpeed Insights 测试网站性能：

```
https://pagespeed.web.dev/
```

目标：
- Performance: > 90
- Accessibility: > 90
- Best Practices: > 90
- SEO: > 90

## 🔄 更新流程

当需要更新网站内容时：

```bash
# 1. 修改文件
nano docs/index.html

# 2. 本地预览（可选）
cd docs
python -m http.server 8000
# 访问 http://localhost:8000

# 3. 提交更改
git add docs/
git commit -m "Update website content"
git push origin main

# 4. 等待自动部署（1-2分钟）
```

## 📞 获取帮助

如果遇到问题：

1. 查看 [GitHub Pages 官方文档](https://docs.github.com/en/pages)
2. 在项目仓库提交 [Issue](https://github.com/cmd4coder/cmd4coder/issues)
3. 查看 GitHub Community 论坛

## ✨ 恭喜！

如果一切顺利，你的 cmd4coder GitHub Pages 现在应该已经成功上线了！

你可以：
- 分享你的网站链接给其他人
- 在项目 README 中添加网站链接
- 在社交媒体上宣传你的项目

**享受你的新网站吧！** 🎉
